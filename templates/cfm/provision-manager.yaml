{{- if .Values.cfm.provisionManager.enabled }}
{{- $c := .Values.cfm.provisionManager }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cfm-provision-manager
  namespace: {{ include "cpd.namespace" . }}
  labels:
    app: provision-manager
    platform: edcv
    type: edcv-app
    {{- include "cpd.labels" . | nindent 4 }}
spec:
  replicas: {{ $c.replicas }}
  selector:
    matchLabels:
      app: provision-manager
  template:
    metadata:
      labels:
        app: provision-manager
        platform: edcv
        type: edcv-app
    spec:
      initContainers:
        {{- include "cpd.waitForPostgres" (dict "db" "cfm" "user" "cfm" "password" "cfm" "ctx" $) | nindent 8 }}
      containers:
        - name: provision-manager
          image: {{ include "cpd.image" (dict "img" $c.image "ctx" $) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: [ "/pmanager" ]
          args: [
            "--mode=debug"
          ]
          envFrom:
            - configMapRef:
                name: telemetry-config
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: config
              mountPath: /etc/appname
              readOnly: true
          {{- with $c.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: config
          configMap:
            name: pm-config
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: provision-manager
  namespace: {{ include "cpd.namespace" . }}
spec:
  selector:
    app: provision-manager
  ports:
    - port: 8080
      targetPort: 8080
      name: http

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: provision-manager
  namespace: {{ include "cpd.namespace" . }}
spec:
  parentRefs:
    - name: {{ .Values.global.gatewayName }}
      kind: Gateway
      sectionName: http
  hostnames:
    - {{ .Values.global.host }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/pm
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /api/v1alpha1
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: jwt-auth-provision-manager-api
      backendRefs:
        - name: provision-manager
          port: 8080
          weight: 1
{{- end }}
